# Agentic SDD Template

Reusable Spec Driven Development harness for Claude Code projects.

Create a project with:

```bash
python create_project.py --config project.yaml
```

Supported profiles:

- `generic`
- `python`
- `node`

Supported optional capabilities:

- `mutation-testing`
- `windows-validation`
